TeamsWithTask(TeamName) :-
    Teams(TeamId, TeamName),
    Sessions(SessionId, TeamId, ContestId, _),
    Runs(_, SessionId, Letter, _, Accepted),
    ContestId = :ContestId,
    Letter = :Letter,
    Accepted = 1.
